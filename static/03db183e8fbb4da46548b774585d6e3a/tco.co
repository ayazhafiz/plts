# +ir -print
# +eval -print
let fact = \n -> 
  let rec inner = \t ->
    if t.0 == 0 then t.1
    else inner {t.0 - 1, t.1 * t.0}
  in
  inner {n, 1}
in fact 10

> +ir -print
> inner: {
> % Stack relative to frame pointer:
> %   -6  #return
> %   -5  t.1
> %   -4  t.0
> %   -3  @old_pc
> %   -2  @old_fp
> %   -1  @old_sp
> inner:
>   push 0
>   push fp[-4]
>   =
>   jmpz else
> then:
>   push fp[-5]
>   store-into fp[-6]
>   ret
> else:
>   push fp[-4]
>   push fp[-5]
>   mul
>   store-into fp[-5]
>   push 1
>   push fp[-4]
>   sub
>   store-into fp[-4]
>   sp-restore-fp
>   jmp inner
> }
> 
> fact: {
> % Stack relative to frame pointer:
> %   -5  #return
> %   -4  n
> %   -3  @old_pc
> %   -2  @old_fp
> %   -1  @old_sp
> fact:
>   sp-add 1
>   push 1
>   push fp[-4]
>   call inner
>   sp-sub 2
>   store-into fp[-5]
>   ret
> }
> 
> @main: {
> % Stack relative to frame pointer:
> %   -4  #return
> %   -3  @old_pc
> %   -2  @old_fp
> %   -1  @old_sp
> @main:
>   sp-add 1
>   push 10
>   call fact
>   sp-sub 1
>   store-into fp[-4]
>   ret
> }

> +eval -print
> 3628800